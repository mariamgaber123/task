on:
  push:
    branches:
      - master 






jobs:
   build:
      runs-on: ubuntu-latest
      
         
      steps:
         - name: 'Testing Github'
           run: pwd

         - name: 'Testing'
           run: echo hello 

         - uses: actions/checkout@v3

         - name: 'listing files'
           run: ls -a

         - name: 'login secrets'
           run: docker login --username ${{secrets.DOCKER_USERNAME}} --password ${{secrets.DOCKER_TOKEN}}
         - name: 'build image'
           run: docker build -t ${{secrets.DOCKER_USERNAME}}/mariam_gaber_2023040247 .

         - name: 'push image'
           run : docker push ${{secrets.DOCKER_USERNAME}}/mariam_gaber_2023040247